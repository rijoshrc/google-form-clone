export const updateFieldById = (
  items: any[],
  id: string | number,
  field: string
) => {};

/**
 * Generate a unique value from current timestamp
 * @returns
 */
export const uniqueValue = () => Date.now();
